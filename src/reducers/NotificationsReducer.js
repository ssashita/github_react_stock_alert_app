import constants from "../constants";

const reducer = (state = {}, action) => {
    let newState = { ...state };
    switch (action.type) {
        
    }
    return newState;
}

export default reducer;